FROM python:3.11-slim

WORKDIR /app

RUN pip install --no-cache-dir \
    fastapi \
    uvicorn \
    kafka-python \
    kubernetes \
    websockets

COPY mcp_server/api.py .
COPY frontend/index.html /app/static/

CMD ["uvicorn", "api:app", "--host", "0.0.0.0", "--port", "8000"]
